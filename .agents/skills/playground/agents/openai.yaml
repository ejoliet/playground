interface:
  display_name: "Playground"
  short_description: "Repo-specific patterns and workflows for playground"
  default_prompt: "Use the playground repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true